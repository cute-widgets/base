import { CuteToggleSwitch } from './toggleswitch.directive';

describe('ToggleSwitchDirective', () => {
  it('should create an instance', () => {
    const directive = new CuteToggleSwitch();
    expect(directive).toBeTruthy();
  });
});
